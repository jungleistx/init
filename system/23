SSH
-in vm setting, add a port-forwarding rule:
name=ssh, protocol=tcp, host port=3022, guest port=22

-make a new user in the vm-server with sudo adduser <username>
ssh -p 3022 username@localhost

GUI
open virtualmachine
press login, enter username and password.
